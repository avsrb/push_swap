#include "push_swap.h"

int		sort(t_stack *stack)
{
//	swap(&stack->a);
//	rotate(&stack->a);
//	reverse_rotate(&stack->a);
	push(stack, 'b');
	push(stack, 'b');
	push(stack, 'b');
	push(stack, 'b');
	push(stack, 'b');
	push(stack, 'b');
	push(stack, 'b');
	push(stack, 'b');













	return (0);
}

//int		swap(t_stack *stack, char name)
//{
//	if (name == 'a')
//	{
//
//
//	}
//}